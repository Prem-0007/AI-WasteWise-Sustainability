import { Recycle, Sprout, Shirt, Cpu, Droplets, ShieldAlert, CircleHelp } from 'lucide-react'

// Maps the icon keys from data/wasteCategories.js to Lucide components.
export const CATEGORY_ICONS = {
  recycle: Recycle,
  sprout: Sprout,
  shirt: Shirt,
  cpu: Cpu,
  droplets: Droplets,
  shieldAlert: ShieldAlert,
  helpCircle: CircleHelp,
}
